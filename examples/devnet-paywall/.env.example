# Copy this file to .env.local and set this to a devnet wallet address you
# control. This example sends its demo payments to this address — you don't
# need its private key, just the address, so you can check it received the
# payment (e.g. on https://explorer.solana.com/?cluster=devnet).
VITE_RECEIVING_WALLET=
